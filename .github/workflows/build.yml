name: Build APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  # ── Gate: run the interactive PTY test before every build ──────────────────
  # Tests the same startup path the Android PTY terminal uses (no --print).
  # Catches login-screen bypass failures and exit-code-1 crashes up front.
  # Skipped automatically when OPENROUTER_TEST_KEY secret is absent (forks).
  test-pty:
    name: PTY auth + proxy gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install build tools for node-pty
        run: sudo apt-get install -y python3 make g++ libx11-dev
      - name: Interactive PTY session test
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_TEST_KEY }}
        run: node .github/scripts/test-pty-session.js

  build:
    name: Build Debug APK
    runs-on: ubuntu-latest
    # Run after the PTY test passes. If the PTY job was skipped (no secret),
    # needs: skipped counts as satisfied so the build still runs.
    needs: [test-pty]
    if: ${{ always() && (needs.test-pty.result == 'success' || needs.test-pty.result == 'skipped') }}

    steps:

      # ── 1. Checkout ─────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Java 17 ──────────────────────────────────────────────────────────
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # ── 3. Cache libnode.so (57 MB per ABI — cache to avoid repeated download)
      - name: Cache nodejs-mobile native libraries
        id: cache-libnode
        uses: actions/cache@v4
        with:
          path: app/src/main/jniLibs
          key: nodejs-mobile-v18.20.4

      # ── 4. Download libnode.so from official nodejs-mobile release ───────────
      - name: Download nodejs-mobile native libraries
        if: steps.cache-libnode.outputs.cache-hit != 'true'
        run: |
          curl -fL \
            "https://github.com/nodejs-mobile/nodejs-mobile/releases/download/v18.20.4/nodejs-mobile-v18.20.4-android.zip" \
            -o /tmp/nodejs-mobile.zip
          mkdir -p \
            app/src/main/jniLibs/arm64-v8a \
            app/src/main/jniLibs/armeabi-v7a
          unzip -p /tmp/nodejs-mobile.zip bin/arm64-v8a/libnode.so \
            > app/src/main/jniLibs/arm64-v8a/libnode.so
          unzip -p /tmp/nodejs-mobile.zip bin/armeabi-v7a/libnode.so \
            > app/src/main/jniLibs/armeabi-v7a/libnode.so
          echo "libnode.so sizes:"
          du -sh app/src/main/jniLibs/*/libnode.so

      # ── 5. Cache Gradle packages ─────────────────────────────────────────────
      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-

      # ── 6. Make scripts executable ───────────────────────────────────────────
      - name: Make gradlew executable
        run: chmod +x gradlew && chmod +x scripts/build-llamaserver.sh

      # ── 7. Install NDK ───────────────────────────────────────────────────────
      - name: Install NDK 25.1.8937393
        run: |
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "ndk;25.1.8937393"

      # ── 8. Build llama-server for Android ARM64 ─────────────────────────────
      - name: Cache llama-server binary
        id: cache-llama
        uses: actions/cache@v4
        with:
          path: .llama-cache
          key: llama-server-b5188-arm64-v2

      - name: Build llama-server
        run: |
          export ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/25.1.8937393
          ./scripts/build-llamaserver.sh

      # ── 9. Build debug APK ───────────────────────────────────────────────────
      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      # ── 9. Upload APK artifact ───────────────────────────────────────────────
      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: ClaudeCodeSetup-debug-${{ github.run_number }}
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 3
          if-no-files-found: error

      # ── 10. Show APK info ────────────────────────────────────────────────────
      - name: Show APK info
        run: |
          APK=app/build/outputs/apk/debug/app-debug.apk
          echo "Build successful"
          echo "APK size: $(du -sh "$APK" | cut -f1)"
          echo "Path: $APK"
